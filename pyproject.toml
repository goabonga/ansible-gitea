# SPDX-License-Identifier: MIT
# Copyright (c) 2026 Chris <goabonga@pm.me>

# This repository ships Ansible content, not a Python distribution. The
# project metadata exists so `uv` can manage the toolchain (ansible-core,
# ansible-lint, yamllint, ruff for scripts/) and so multicz has a single
# place to write the version. `package = false` keeps uv from trying to
# build a wheel.

[project]
name = "ansible-gitea"
version = "0.1.0"
description = "Ansible project deploying a local Gitea lab on KVM/QEMU with Gitea Actions runners."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Chris", email = "goabonga@pm.me" }]
keywords = ["ansible", "gitea", "kvm", "libvirt", "qemu", "act-runner"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: System Administrators",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: System :: Installation/Setup",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/goabonga/ansible-gitea"
Repository = "https://github.com/goabonga/ansible-gitea"
Issues = "https://github.com/goabonga/ansible-gitea/issues"
Changelog = "https://github.com/goabonga/ansible-gitea/blob/main/CHANGELOG.md"

[tool.uv]
package = false

[dependency-groups]
dev = [
    "ansible-core>=2.19.12",
    "ansible-lint>=26.8.0",
    "yamllint>=1.35",
    "ruff>=0.16.3",
    "pre-commit>=4.6.2",
]

[tool.ruff]
target-version = "py311"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
