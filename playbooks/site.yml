# SPDX-License-Identifier: MIT
# Copyright (c) 2026 Chris <goabonga@pm.me>

# Full lab, from a bare workstation to runners waiting for jobs:
#
#   uv run ansible-playbook playbooks/site.yml --ask-become-pass
#
# The sudo password is only needed for the host-side plays (packages,
# libvirt, guest disks); the guest account is passwordless.
---

- name: Prepare the KVM host
  ansible.builtin.import_playbook: kvm-host.yml

- name: Provision the lab guests
  ansible.builtin.import_playbook: provision.yml

# Optional, and a no-op unless `lab_services_enabled` is true. It runs before
# Gitea because that is where the CA, the DNS records and the trust store come
# from when Gitea is served over https.
- name: Deploy the internal services
  ansible.builtin.import_playbook: services.yml

- name: Deploy Gitea
  ansible.builtin.import_playbook: gitea.yml

- name: Deploy the Gitea Actions agents
  ansible.builtin.import_playbook: runners.yml
